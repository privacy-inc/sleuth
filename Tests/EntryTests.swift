import XCTest
@testable import Sleuth

final class EntryTests: XCTestCase {
    func testId() {
        let id = 34567
//        XCTAssertEqual(id, Entry(id: id, url: "").data.mutating(transform: Entry.init(data:)).id)
    }
    
    func testProperties() {
//        let date = Date()
//        let url = "https://www.aguacate.com:8080/asd/124?page=32123&lsd=1"
//        let entry = Entry(id: 0, url: url)
//                        .with(title: "Aguacate")
//        XCTAssertGreaterThanOrEqual(entry.data.mutating(transform: Entry.Info.init(data:)).date.timestamp, date.timestamp)
//        XCTAssertEqual(url, entry.data.mutating(transform: Entry.Info.init(data:)).url)
//        XCTAssertEqual("Aguacate", entry.data.mutating(transform: Entry.Info.init(data:)).title)
    }
}
