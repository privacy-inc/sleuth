import Foundation

extension URL.White {
    enum Subdomain: String {
        case
        platform,
        interactive,
        tr,
        accounts,
        tarifvergleich,
        mobileads,
        sourcepointcmp,
        tracking
    }
}
